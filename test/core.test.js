import test from 'node:test'; import assert from 'node:assert/strict'; import {DevServerRegistry,validateDevCommand} from '../src/core.js'; test('tracks server lifecycle',()=>{const r=new DevServerRegistry(); r.register({id:'web',command:'npm run dev',pid:10}); r.update('web',{status:'stopped'}); assert.equal(r.get('web').status,'stopped');}); test('blocks compound shell syntax',()=>assert.throws(()=>validateDevCommand('npm run dev; rm -rf x'))); test('accepts simple command',()=>assert.equal(validateDevCommand('npm run dev'),'npm run dev'));
