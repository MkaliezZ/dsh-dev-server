import {DevServerRegistry,validateDevCommand} from './core.js'; export function registerDevServer(ctx,{start,stop,logs},registry=new DevServerRegistry()){ctx.command?.('dev-start',async(id,...parts)=>{const command=validateDevCommand(parts.join(' ')); const h=await start(command); registry.register({id,command,pid:h?.pid??null}); return JSON.stringify(registry.get(id));}); ctx.command?.('dev-list',async()=>JSON.stringify(registry.list(),null,2)); ctx.command?.('dev-stop',async(id)=>{const item=registry.get(id); if(!item) throw new Error('server not found'); await stop(item); registry.update(id,{status:'stopped'}); return 'stopped';}); ctx.command?.('dev-logs',async(id)=>logs(registry.get(id))); return registry;}
